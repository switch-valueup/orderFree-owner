<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.userapplication">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:usesCleartextTraffic="true">

        <activity android:name=".login.activity.SplashActivity">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        </activity>
        <activity android:name=".login.activity.LoginActivity"/>
        <activity android:name=".login.activity.JoinActivity" />
        <activity android:name=".login.activity.MainActivity" />
        <activity android:name=".login.activity.FindEmailActivity"/>
        <activity android:name=".login.activity.FindEmailResultActivity"/>
        <activity android:name=".login.activity.FindPasswordActivity"/>
        <activity android:name=".mainview.activity.addMenuActivity"/>
        <activity android:name=".mainview.activity.addMenuResultActivity"/>
        <activity android:name=".mainview.activity.personInfoActivity"/>
        <activity android:name=".mainview.activity.SellStatusActivity"/>
        <activity android:name=".mainview.activity.daumWebViewActivity"/>
        <activity android:name=".login.activity.AccountDeleteActivity"/>
        <activity android:name=".login.activity.ChangePasswordActivity" android:theme="@android:style/Theme.Dialog" />

        <service android:name=".MyFireBaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT"/>
            </intent-filter>
        </service>
    </application>
</manifest>